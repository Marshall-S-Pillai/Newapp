echo_goodbye:
  runs-on: ubuntu-latest
  steps:
    - name: Failed step
      run: |
       echo "I am"
       echo "a multi-line"
       echo "script."
       exit 1

    - name: Say goodbye
      run:
       echo "Goodbye!"
       exit 0
