echo_goodbye:
  runs-on: ubuntu-latest
  steps:
    - name: Failed step
      run: 
       echo "I will fail "
       echo "I will fail"
       echo "I will fail"
       exit 0

    - name: Say goodbye
      run:
       echo "Goodbye!"
       exit 0
